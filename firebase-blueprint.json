{
  "entities": {
    "SecurityEvent": {
      "title": "Security Event",
      "description": "An AI-detected security incident recorded from a camera feed.",
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": ["shoplifting", "fall", "suspicious", "error", "normal"],
          "description": "The category of the incident."
        },
        "description": {
          "type": "string",
          "description": "Detailed explanation of the behavior observed."
        },
        "confidence": {
          "type": "number",
          "description": "AI confidence level (0 to 1)."
        },
        "imageUrl": {
          "type": "string",
          "description": "Base64 or URL of the frame that triggered the detection."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "When the event occurred."
        }
      },
      "required": ["type", "description", "confidence", "imageUrl", "timestamp"]
    }
  },
  "firestore": {
    "/users/{userId}/events": {
      "schema": "SecurityEvent",
      "description": "Chronological log of security events for a specific store/user."
    }
  }
}
